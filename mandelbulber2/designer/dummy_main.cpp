/**
 * Dummy main.cpp for Designer project
 * This file is never actually compiled — it's only here so qmake
 * generates a valid project file that Qt Designer can open.
 */

#include <QApplication>

int main(int argc, char *argv[])
{
	QApplication app(argc, argv);
	return 0;
}
