/**
 * Mandelbulber v2, a 3D fractal generator
 *
 * Plane Clipping Transform
 * AUTO-GENERATED by Transform Clipping Tool
 */

#include "all_fractal_definitions.h"

class cFractalClipPlane000000000 : public cAbstractFractal
{
public:
	cFractalClipPlane000000000();
	void FormulaCode(CVector4 &z, const sFractal *fractal, sExtendedAux &aux) override;
};

cFractalClipPlane000000000::cFractalClipPlane000000000() : cAbstractFractal()
{
	nameInComboBox = "T>Clip cFractalClipPlane000000000";
	internalName = "clip_plane_000_000_000";
	internalID = fractal::clipPlane;
	DEType = analyticDEType;
	DEFunctionType = customDEFunction;
	cpixelAddition = cpixelDisabledByDefault;
	defaultBailout = 100.0;
	DEAnalyticFunction = analyticFunctionCustomDE;
	coloringFunction = coloringFunctionDefault;
}

void cFractalClipPlane000000000::FormulaCode(
	CVector4 &z, const sFractal *fractal, sExtendedAux &aux)
{
	// Plane clipping
	CVector3 normal = CVector3(0.0, 0.0, 1.0);
	normal.Normalize();
	
	CVector3 planePoint = CVector3(0.0, 0.0, 0.0);
	
	// Distance to plane
	double dist = (z - planePoint).Dot(normal);
	
	
	// Apply clipping
	if (dist < 0.1)
	{
		aux.dist = min(aux.dist, fabs(dist - 0.1));
		aux.DE = min(aux.DE, fabs(dist - 0.1));
	}
}
